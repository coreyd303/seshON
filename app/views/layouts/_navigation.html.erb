<div class="navbar navbar-default navbar-fixed-top z-index:10;" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <p><%= link_to "Seshon", home_path, class: "masthead-brand navbar-brand font-effect-3d" %></p>
    </div>
    <div class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li>&nbsp</li>
        <li>
          <%= link_to "Find Skate Parks", skate_maps_path %>
        </li>
        <li>
          <% if current_user %>
            <%= link_to "User SeshON Spots", user_skate_spots_path %>
          <% end %>
        </li>
        <li>
          <% if current_user %>
            <%= link_to "Current Feature Seshon", home_path %>
          <% end %>
        </li>
      </ul>


      <ul class="nav navbar-nav navbar-right">
        <li>
          <% if current_user %>
            <%= link_to "Welcome '#{current_user.name}'", root_path %>
          <% else %>
            <%= link_to "Sign In with Facebook", 'auth/facebook' %>
          <% end %>
        </li>
        <li>
          <% if current_user %>
            <%= link_to "Sign Out", signout_path %>
          <% end %>
        </li>
        <li>
          <% if current_user && current_user.role == "admin" %>
            <%= link_to "Admin Dashboard", administrator_admins_path %>
          <% end %>
        </li>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</div>







